import 'package:flutter/material.dart';

class UVIndexPage extends StatelessWidget {
  const UVIndexPage({super.key});

  @override
  Widget build(BuildContext conext) {
    return const Center(
      child: Text('UV Index Page', style: TextStyle(fontSize: 24)),
    );
  }
}
