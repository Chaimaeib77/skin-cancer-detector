import 'package:flutter/material.dart';

class EDoctorPage extends StatelessWidget {
  const EDoctorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('E-Doctor Page', style: TextStyle(fontSize: 24)),
    );
  }
}
